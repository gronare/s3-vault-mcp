[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "s3-vault-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "boto3>=1.34.0",
    "pyyaml>=6.0",
]

[project.scripts]
s3-vault-mcp = "s3_vault_mcp:main"
